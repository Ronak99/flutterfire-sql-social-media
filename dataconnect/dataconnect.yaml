specVersion: "v1beta"
serviceId: "my-data-connect"
location: "asia-south1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "my-data"
      cloudSql:
        instanceId: "my-cloud-sql"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
